version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
  disable:
    - depguard
  settings:
    errcheck:
      check-blank: false
  exclusions:
    rules:
      - linters: [errcheck]
        text: "rows.Close"
      - linters: [errcheck]
        text: "rows.Scan"
